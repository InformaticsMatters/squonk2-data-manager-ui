/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * Account Server API
 * The Informatics Matters Account Server API.
 *
 * A service that provides access to the Account Server, which gives *registered* users access to and management of **Organisations**, **Units**, **Products**, **Users**, and **Assets**.
 *
 * OpenAPI spec version: 4.7
 */
import * as zod from "zod";

/**
 * Gets Product Types you can purchase (subscribe to)
 * @summary Gets all Product Types
 */
export const AppApiProductGetTypesResponse = zod.object({
  count: zod.number().describe("The number of Product Types"),
  product_types: zod
    .array(
      zod.object({
        type: zod
          .string()
          .describe(
            "A product type, this is a unique string amongst all types known to the Account Server",
          ),
        flavour: zod
          .string()
          .optional()
          .describe(
            "A product flavour. Not all types have a flavour, those that do have a type-specific flavour string",
          ),
        service_kind: zod
          .enum(["DATA_MANAGER"])
          .optional()
          .describe("The kind of service that can use the Product"),
      }),
    )
    .describe("All the Product Types you have access to"),
});

/**
 * The storage cost is returned along with its measurement units, typically a Coin value based on the the peak storage that was measured on each billable day.
 * @summary Gets the default cross-product storage cost
 */
export const AppApiProductGetDefaultStorageCostResponse = zod.object({
  default_storage_cost: zod.object({
    cost: zod.string(),
    units: zod.string(),
    description: zod.string(),
  }),
});

/**
 * Gets Products you have access to, across all **Units** and **Organisations**
 * @summary Gets all Products
 */
export const AppApiProductGetResponse = zod.object({
  count: zod.number(),
  products: zod
    .array(
      zod.union([
        zod.object({
          product: zod.object({
            id: zod.string().describe("The Product ID\n"),
            created: zod.iso.datetime({ offset: true }),
            type: zod
              .enum(["DATA_MANAGER_PROJECT_TIER_SUBSCRIPTION", "DATA_MANAGER_STORAGE_SUBSCRIPTION"])
              .describe("The Product Type\n"),
            flavour: zod
              .enum(["EVALUATION", "BRONZE", "SILVER", "GOLD"])
              .optional()
              .describe("The Product Type flavour. Not all products have flavours\n"),
            name: zod.string().optional().describe("The name of the Product\n"),
          }),
          organisation: zod.object({
            caller_is_member: zod
              .boolean()
              .describe("Whether the user making the API call is a member of the Unit"),
            id: zod.string().describe("The Organisation's unique ID"),
            name: zod.string().describe("The Organisation's name"),
            owner_id: zod
              .string()
              .optional()
              .describe(
                "The username of the Organisation's owner. Not all Organisations have an owner. The Default Organisation has no owner.",
              ),
            private: zod.boolean().describe("True if the Unit is private"),
            created: zod.iso.datetime({ offset: true }),
            default_product_privacy: zod
              .enum(["ALWAYS_PUBLIC", "ALWAYS_PRIVATE", "DEFAULT_PUBLIC", "DEFAULT_PRIVATE"])
              .describe("The Organisation's default product privacy setting"),
            users: zod
              .array(zod.object({ id: zod.string().describe("The user identity (username)") }))
              .describe("A list of users that are members of the Organisation"),
          }),
          unit: zod.object({
            caller_is_member: zod
              .boolean()
              .describe("Whether the user making the API call is a member of the Unit"),
            id: zod.string().describe("The Unit's unique identity"),
            name: zod.string().describe("The Unit's name"),
            owner_id: zod.string().describe("The Unit's owner (a username)"),
            private: zod.boolean().describe("True if the Unit is private"),
            created: zod.iso.datetime({ offset: true }),
            billing_day: zod.number().describe("The Unit's billing day"),
            default_product_privacy: zod
              .enum(["ALWAYS_PUBLIC", "ALWAYS_PRIVATE", "DEFAULT_PUBLIC", "DEFAULT_PRIVATE"])
              .describe("The Unit's default product privacy setting"),
            users: zod
              .array(zod.object({ id: zod.string().describe("The user identity (username)") }))
              .describe("A list of users that are members of the Unit"),
          }),
          storage: zod.object({
            size: zod.object({
              peak: zod
                .string()
                .describe(
                  "The humanised size of the peak storage used for the current day. The value is reset at the start of each day",
                ),
              current: zod
                .string()
                .describe(
                  "The humanised size of the current storage used for the current day and used to calculate the 'burn rate'",
                ),
              unit_size: zod
                .string()
                .describe(
                  "The humanised storage unit. The cost of storage is based on the daily peak of the number of units (or part thereof) used",
                ),
              units_used: zod.number().describe("The peak number of storage units used today"),
            }),
            coins: zod.object({
              used: zod
                .number()
                .describe(
                  "The number of coins currently committed for the current day. This is added to the accumulated coins at the start of each day",
                ),
              unit_cost: zod
                .number()
                .describe(
                  "The coin cost of a 'unit' of storage or part thereof. The unit size is defined in the storage section of the response",
                ),
            }),
          }),
          coins: zod.object({
            allowance: zod
              .number()
              .describe(
                "The billing allowance. When you exceed this during the current billing period the \*cost multiplier\* will increase",
              ),
            limit: zod
              .number()
              .describe(
                "The limit on your billing period spend. You can exceed the allowance but you cannot exceed the spend limit. Once reached the dependent may be restricted",
              ),
            used: zod
              .number()
              .describe(
                "The total number of coins consumed (in this billing period), excluding the coins that have been consumed for the current day",
              ),
            at_limit: zod
              .boolean()
              .describe(
                "True if the product is operating at or beyond its coin limit. When it is authority to perform actions using the product are severely limited.",
              ),
            current_burn_rate: zod
              .number()
              .describe(
                "The current burn rate, the approximate amount of coins you are currently consuming each day",
              ),
            billing_prediction: zod
              .number()
              .describe(
                "The predicted total billing period amount, if costs continue at the current burn rate until the end of the billing period. This is an estimate",
              ),
            billing_prediction_storage_contribution: zod
              .number()
              .describe(
                "The predicted storage contribution to the billing period amount, if costs continue at the current burn rate until the end of the billing period. This is an estimate",
              ),
            billing_day: zod
              .number()
              .describe(
                "The day of the month when the bill is due, and the end of the current billing period",
              ),
            allowance_multiplier: zod
              .number()
              .describe("A multiplier applied to your coin usage within your allowance"),
            overspend_multiplier: zod
              .number()
              .describe("A multiplier that will be applied to coin used beyond your allowance"),
            remaining_days: zod
              .number()
              .describe("The number of days remaining, in the current billing period"),
          }),
          claimable: zod.boolean().describe("True if the product can be (needs to be) claimed."),
        }),
        zod.object({
          product: zod.object({
            id: zod.string().describe("The Product ID\n"),
            created: zod.iso.datetime({ offset: true }),
            type: zod
              .enum(["DATA_MANAGER_PROJECT_TIER_SUBSCRIPTION", "DATA_MANAGER_STORAGE_SUBSCRIPTION"])
              .describe("The Product Type\n"),
            flavour: zod
              .enum(["EVALUATION", "BRONZE", "SILVER", "GOLD"])
              .optional()
              .describe("The Product Type flavour. Not all products have flavours\n"),
            name: zod.string().optional().describe("The name of the Product\n"),
          }),
          organisation: zod.object({
            caller_is_member: zod
              .boolean()
              .describe("Whether the user making the API call is a member of the Unit"),
            id: zod.string().describe("The Organisation's unique ID"),
            name: zod.string().describe("The Organisation's name"),
            owner_id: zod
              .string()
              .optional()
              .describe(
                "The username of the Organisation's owner. Not all Organisations have an owner. The Default Organisation has no owner.",
              ),
            private: zod.boolean().describe("True if the Unit is private"),
            created: zod.iso.datetime({ offset: true }),
            default_product_privacy: zod
              .enum(["ALWAYS_PUBLIC", "ALWAYS_PRIVATE", "DEFAULT_PUBLIC", "DEFAULT_PRIVATE"])
              .describe("The Organisation's default product privacy setting"),
            users: zod
              .array(zod.object({ id: zod.string().describe("The user identity (username)") }))
              .describe("A list of users that are members of the Organisation"),
          }),
          unit: zod.object({
            caller_is_member: zod
              .boolean()
              .describe("Whether the user making the API call is a member of the Unit"),
            id: zod.string().describe("The Unit's unique identity"),
            name: zod.string().describe("The Unit's name"),
            owner_id: zod.string().describe("The Unit's owner (a username)"),
            private: zod.boolean().describe("True if the Unit is private"),
            created: zod.iso.datetime({ offset: true }),
            billing_day: zod.number().describe("The Unit's billing day"),
            default_product_privacy: zod
              .enum(["ALWAYS_PUBLIC", "ALWAYS_PRIVATE", "DEFAULT_PUBLIC", "DEFAULT_PRIVATE"])
              .describe("The Unit's default product privacy setting"),
            users: zod
              .array(zod.object({ id: zod.string().describe("The user identity (username)") }))
              .describe("A list of users that are members of the Unit"),
          }),
          storage: zod.object({
            size: zod.object({
              peak: zod
                .string()
                .describe(
                  "The humanised size of the peak storage used for the current day. The value is reset at the start of each day",
                ),
              current: zod
                .string()
                .describe(
                  "The humanised size of the current storage used for the current day and used to calculate the 'burn rate'",
                ),
              unit_size: zod
                .string()
                .describe(
                  "The humanised storage unit. The cost of storage is based on the daily peak of the number of units (or part thereof) used",
                ),
              units_used: zod.number().describe("The peak number of storage units used today"),
            }),
            coins: zod.object({
              used: zod
                .number()
                .describe(
                  "The number of coins currently committed for the current day. This is added to the accumulated coins at the start of each day",
                ),
              unit_cost: zod
                .number()
                .describe(
                  "The coin cost of a 'unit' of storage or part thereof. The unit size is defined in the storage section of the response",
                ),
            }),
          }),
          coins: zod.object({
            allowance: zod
              .number()
              .describe(
                "The billing allowance. When you exceed this during the current billing period the \*cost multiplier\* will increase",
              ),
            limit: zod
              .number()
              .describe(
                "The limit on your billing period spend. You can exceed the allowance but you cannot exceed the spend limit. Once reached the dependent may be restricted",
              ),
            used: zod
              .number()
              .describe(
                "The total number of coins consumed (in this billing period), excluding the coins that have been consumed for the current day",
              ),
            at_limit: zod
              .boolean()
              .describe(
                "True if the product is operating at or beyond its coin limit. When it is authority to perform actions using the product are severely limited.",
              ),
            current_burn_rate: zod
              .number()
              .describe(
                "The current burn rate, the approximate amount of coins you are currently consuming each day",
              ),
            billing_prediction: zod
              .number()
              .describe(
                "The predicted total billing period amount, if costs continue at the current burn rate until the end of the billing period. This is an estimate",
              ),
            billing_prediction_storage_contribution: zod
              .number()
              .describe(
                "The predicted storage contribution to the billing period amount, if costs continue at the current burn rate until the end of the billing period. This is an estimate",
              ),
            billing_day: zod
              .number()
              .describe(
                "The day of the month when the bill is due, and the end of the current billing period",
              ),
            allowance_multiplier: zod
              .number()
              .describe("A multiplier applied to your coin usage within your allowance"),
            overspend_multiplier: zod
              .number()
              .describe("A multiplier that will be applied to coin used beyond your allowance"),
            remaining_days: zod
              .number()
              .describe("The number of days remaining, in the current billing period"),
          }),
          instance: zod.object({
            coins: zod.object({ used: zod.number().describe("The number of coins used\n") }),
          }),
          claimable: zod.boolean().describe("True if the product can be (needs to be) claimed."),
          claim: zod
            .object({
              id: zod
                .string()
                .describe("The service-specific ID that is using this Subscription\n"),
              name: zod.string().optional().describe("A name for the service-specific ID\n"),
            })
            .optional(),
        }),
      ]),
    )
    .describe("All the Products you have access to"),
});

/**
 * Gets Products you have access to based on an **Organisation**
 * @summary Gets Products for an Organisation
 */
export const appApiProductGetForOrganisationPathOrgIdRegExp = new RegExp(
  "^org-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiProductGetForOrganisationParams = zod.object({
  org_id: zod
    .string()
    .regex(appApiProductGetForOrganisationPathOrgIdRegExp)
    .describe("An Organisation Identity"),
});

export const AppApiProductGetForOrganisationResponse = zod.object({
  count: zod.number(),
  products: zod
    .array(
      zod.union([
        zod.object({
          product: zod.object({
            id: zod.string().describe("The Product ID\n"),
            created: zod.iso.datetime({ offset: true }),
            type: zod
              .enum(["DATA_MANAGER_PROJECT_TIER_SUBSCRIPTION", "DATA_MANAGER_STORAGE_SUBSCRIPTION"])
              .describe("The Product Type\n"),
            flavour: zod
              .enum(["EVALUATION", "BRONZE", "SILVER", "GOLD"])
              .optional()
              .describe("The Product Type flavour. Not all products have flavours\n"),
            name: zod.string().optional().describe("The name of the Product\n"),
          }),
          organisation: zod.object({
            caller_is_member: zod
              .boolean()
              .describe("Whether the user making the API call is a member of the Unit"),
            id: zod.string().describe("The Organisation's unique ID"),
            name: zod.string().describe("The Organisation's name"),
            owner_id: zod
              .string()
              .optional()
              .describe(
                "The username of the Organisation's owner. Not all Organisations have an owner. The Default Organisation has no owner.",
              ),
            private: zod.boolean().describe("True if the Unit is private"),
            created: zod.iso.datetime({ offset: true }),
            default_product_privacy: zod
              .enum(["ALWAYS_PUBLIC", "ALWAYS_PRIVATE", "DEFAULT_PUBLIC", "DEFAULT_PRIVATE"])
              .describe("The Organisation's default product privacy setting"),
            users: zod
              .array(zod.object({ id: zod.string().describe("The user identity (username)") }))
              .describe("A list of users that are members of the Organisation"),
          }),
          unit: zod.object({
            caller_is_member: zod
              .boolean()
              .describe("Whether the user making the API call is a member of the Unit"),
            id: zod.string().describe("The Unit's unique identity"),
            name: zod.string().describe("The Unit's name"),
            owner_id: zod.string().describe("The Unit's owner (a username)"),
            private: zod.boolean().describe("True if the Unit is private"),
            created: zod.iso.datetime({ offset: true }),
            billing_day: zod.number().describe("The Unit's billing day"),
            default_product_privacy: zod
              .enum(["ALWAYS_PUBLIC", "ALWAYS_PRIVATE", "DEFAULT_PUBLIC", "DEFAULT_PRIVATE"])
              .describe("The Unit's default product privacy setting"),
            users: zod
              .array(zod.object({ id: zod.string().describe("The user identity (username)") }))
              .describe("A list of users that are members of the Unit"),
          }),
          storage: zod.object({
            size: zod.object({
              peak: zod
                .string()
                .describe(
                  "The humanised size of the peak storage used for the current day. The value is reset at the start of each day",
                ),
              current: zod
                .string()
                .describe(
                  "The humanised size of the current storage used for the current day and used to calculate the 'burn rate'",
                ),
              unit_size: zod
                .string()
                .describe(
                  "The humanised storage unit. The cost of storage is based on the daily peak of the number of units (or part thereof) used",
                ),
              units_used: zod.number().describe("The peak number of storage units used today"),
            }),
            coins: zod.object({
              used: zod
                .number()
                .describe(
                  "The number of coins currently committed for the current day. This is added to the accumulated coins at the start of each day",
                ),
              unit_cost: zod
                .number()
                .describe(
                  "The coin cost of a 'unit' of storage or part thereof. The unit size is defined in the storage section of the response",
                ),
            }),
          }),
          coins: zod.object({
            allowance: zod
              .number()
              .describe(
                "The billing allowance. When you exceed this during the current billing period the \*cost multiplier\* will increase",
              ),
            limit: zod
              .number()
              .describe(
                "The limit on your billing period spend. You can exceed the allowance but you cannot exceed the spend limit. Once reached the dependent may be restricted",
              ),
            used: zod
              .number()
              .describe(
                "The total number of coins consumed (in this billing period), excluding the coins that have been consumed for the current day",
              ),
            at_limit: zod
              .boolean()
              .describe(
                "True if the product is operating at or beyond its coin limit. When it is authority to perform actions using the product are severely limited.",
              ),
            current_burn_rate: zod
              .number()
              .describe(
                "The current burn rate, the approximate amount of coins you are currently consuming each day",
              ),
            billing_prediction: zod
              .number()
              .describe(
                "The predicted total billing period amount, if costs continue at the current burn rate until the end of the billing period. This is an estimate",
              ),
            billing_prediction_storage_contribution: zod
              .number()
              .describe(
                "The predicted storage contribution to the billing period amount, if costs continue at the current burn rate until the end of the billing period. This is an estimate",
              ),
            billing_day: zod
              .number()
              .describe(
                "The day of the month when the bill is due, and the end of the current billing period",
              ),
            allowance_multiplier: zod
              .number()
              .describe("A multiplier applied to your coin usage within your allowance"),
            overspend_multiplier: zod
              .number()
              .describe("A multiplier that will be applied to coin used beyond your allowance"),
            remaining_days: zod
              .number()
              .describe("The number of days remaining, in the current billing period"),
          }),
          claimable: zod.boolean().describe("True if the product can be (needs to be) claimed."),
        }),
        zod.object({
          product: zod.object({
            id: zod.string().describe("The Product ID\n"),
            created: zod.iso.datetime({ offset: true }),
            type: zod
              .enum(["DATA_MANAGER_PROJECT_TIER_SUBSCRIPTION", "DATA_MANAGER_STORAGE_SUBSCRIPTION"])
              .describe("The Product Type\n"),
            flavour: zod
              .enum(["EVALUATION", "BRONZE", "SILVER", "GOLD"])
              .optional()
              .describe("The Product Type flavour. Not all products have flavours\n"),
            name: zod.string().optional().describe("The name of the Product\n"),
          }),
          organisation: zod.object({
            caller_is_member: zod
              .boolean()
              .describe("Whether the user making the API call is a member of the Unit"),
            id: zod.string().describe("The Organisation's unique ID"),
            name: zod.string().describe("The Organisation's name"),
            owner_id: zod
              .string()
              .optional()
              .describe(
                "The username of the Organisation's owner. Not all Organisations have an owner. The Default Organisation has no owner.",
              ),
            private: zod.boolean().describe("True if the Unit is private"),
            created: zod.iso.datetime({ offset: true }),
            default_product_privacy: zod
              .enum(["ALWAYS_PUBLIC", "ALWAYS_PRIVATE", "DEFAULT_PUBLIC", "DEFAULT_PRIVATE"])
              .describe("The Organisation's default product privacy setting"),
            users: zod
              .array(zod.object({ id: zod.string().describe("The user identity (username)") }))
              .describe("A list of users that are members of the Organisation"),
          }),
          unit: zod.object({
            caller_is_member: zod
              .boolean()
              .describe("Whether the user making the API call is a member of the Unit"),
            id: zod.string().describe("The Unit's unique identity"),
            name: zod.string().describe("The Unit's name"),
            owner_id: zod.string().describe("The Unit's owner (a username)"),
            private: zod.boolean().describe("True if the Unit is private"),
            created: zod.iso.datetime({ offset: true }),
            billing_day: zod.number().describe("The Unit's billing day"),
            default_product_privacy: zod
              .enum(["ALWAYS_PUBLIC", "ALWAYS_PRIVATE", "DEFAULT_PUBLIC", "DEFAULT_PRIVATE"])
              .describe("The Unit's default product privacy setting"),
            users: zod
              .array(zod.object({ id: zod.string().describe("The user identity (username)") }))
              .describe("A list of users that are members of the Unit"),
          }),
          storage: zod.object({
            size: zod.object({
              peak: zod
                .string()
                .describe(
                  "The humanised size of the peak storage used for the current day. The value is reset at the start of each day",
                ),
              current: zod
                .string()
                .describe(
                  "The humanised size of the current storage used for the current day and used to calculate the 'burn rate'",
                ),
              unit_size: zod
                .string()
                .describe(
                  "The humanised storage unit. The cost of storage is based on the daily peak of the number of units (or part thereof) used",
                ),
              units_used: zod.number().describe("The peak number of storage units used today"),
            }),
            coins: zod.object({
              used: zod
                .number()
                .describe(
                  "The number of coins currently committed for the current day. This is added to the accumulated coins at the start of each day",
                ),
              unit_cost: zod
                .number()
                .describe(
                  "The coin cost of a 'unit' of storage or part thereof. The unit size is defined in the storage section of the response",
                ),
            }),
          }),
          coins: zod.object({
            allowance: zod
              .number()
              .describe(
                "The billing allowance. When you exceed this during the current billing period the \*cost multiplier\* will increase",
              ),
            limit: zod
              .number()
              .describe(
                "The limit on your billing period spend. You can exceed the allowance but you cannot exceed the spend limit. Once reached the dependent may be restricted",
              ),
            used: zod
              .number()
              .describe(
                "The total number of coins consumed (in this billing period), excluding the coins that have been consumed for the current day",
              ),
            at_limit: zod
              .boolean()
              .describe(
                "True if the product is operating at or beyond its coin limit. When it is authority to perform actions using the product are severely limited.",
              ),
            current_burn_rate: zod
              .number()
              .describe(
                "The current burn rate, the approximate amount of coins you are currently consuming each day",
              ),
            billing_prediction: zod
              .number()
              .describe(
                "The predicted total billing period amount, if costs continue at the current burn rate until the end of the billing period. This is an estimate",
              ),
            billing_prediction_storage_contribution: zod
              .number()
              .describe(
                "The predicted storage contribution to the billing period amount, if costs continue at the current burn rate until the end of the billing period. This is an estimate",
              ),
            billing_day: zod
              .number()
              .describe(
                "The day of the month when the bill is due, and the end of the current billing period",
              ),
            allowance_multiplier: zod
              .number()
              .describe("A multiplier applied to your coin usage within your allowance"),
            overspend_multiplier: zod
              .number()
              .describe("A multiplier that will be applied to coin used beyond your allowance"),
            remaining_days: zod
              .number()
              .describe("The number of days remaining, in the current billing period"),
          }),
          instance: zod.object({
            coins: zod.object({ used: zod.number().describe("The number of coins used\n") }),
          }),
          claimable: zod.boolean().describe("True if the product can be (needs to be) claimed."),
          claim: zod
            .object({
              id: zod
                .string()
                .describe("The service-specific ID that is using this Subscription\n"),
              name: zod.string().optional().describe("A name for the service-specific ID\n"),
            })
            .optional(),
        }),
      ]),
    )
    .describe("All the Products you have access to"),
});

/**
 * Products are **Subscriptions** that you create in a **Unit** that allow you to use services provided by a **Merchant**. To create products you need to be a member of the **Unit** or the Unit's **Organisation**.
 *
 * Supported subscription **types** include `DATA_MANAGER_STORAGE_SUBSCRIPTION`, and `DATA_MANAGER_PROJECT_TIER_SUBSCRIPTION`.
 *
 * The `DATA_MANAGER_STORAGE_SUBSCRIPTION` **type** needs a **name**, an **allowance**, and an optional **limit** (that cannot be less than the **allowance**). If no **limit** is provided the **allowance** is used.
 *
 * The `DATA_MANAGER_PROJECT_TIER_SUBSCRIPTION` **type** needs a **name**, and a **flavour**. The **flavour**, a string, should typically be one of `EVALUATION`, `BRONZE`, `SILVER` or `GOLD`.
 *
 * Users who are **Evaluators** can only create products in their Personal Unit, and where products support flavours, are restricted to `EVALUATION` flavours .
 * @summary Creates a Product for a Unit
 */
export const appApiProductPostPathUnitIdRegExp = new RegExp(
  "^unit-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiProductPostParams = zod.object({
  unit_id: zod.string().regex(appApiProductPostPathUnitIdRegExp).describe("A Unit Identity"),
});

export const appApiProductPostBodyNameMax = 80;

export const AppApiProductPostBody = zod.object({
  name: zod
    .string()
    .max(appApiProductPostBodyNameMax)
    .describe("The name you want to give the Product"),
  type: zod
    .enum(["DATA_MANAGER_PROJECT_TIER_SUBSCRIPTION", "DATA_MANAGER_STORAGE_SUBSCRIPTION"])
    .describe(
      "The Type of Product. Storage subscriptions require an \*\*Allowance\*\* to be defined and the \*\*Flavour\*\* must not be provided.\n\nProject Tier subscriptions have built-in allowances and Limits so you must not provide values for these for these products",
    ),
  flavour: zod
    .enum(["EVALUATION", "BRONZE", "SILVER", "GOLD"])
    .optional()
    .describe(
      "The Flavour of the Product. Used only for Project Tier Products. Do not set this for Storage products",
    ),
  allowance: zod
    .number()
    .min(1)
    .optional()
    .describe(
      "The Product's coin allowance. You must provide this for Storage products but you must not provide a value for Project Tier Products",
    ),
  limit: zod
    .number()
    .min(1)
    .optional()
    .describe(
      "The Product's built-in coin limit. If set it must not be less than the allowance. If not set the allowance is used. You can provide this for Storage products but you must not provide a value for Project Tier Products",
    ),
});

export const AppApiProductPostResponse = zod.object({
  id: zod.string().describe("The Product's unique ID"),
});

/**
 * Gets products you have access to based on an Organisational **Unit**
 * @summary Gets Products for an Organisational Unit
 */
export const appApiProductGetForUnitPathUnitIdRegExp = new RegExp(
  "^unit-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiProductGetForUnitParams = zod.object({
  unit_id: zod.string().regex(appApiProductGetForUnitPathUnitIdRegExp).describe("A Unit Identity"),
});

export const AppApiProductGetForUnitResponse = zod.object({
  count: zod.number(),
  products: zod
    .array(
      zod.union([
        zod.object({
          product: zod.object({
            id: zod.string().describe("The Product ID\n"),
            created: zod.iso.datetime({ offset: true }),
            type: zod
              .enum(["DATA_MANAGER_PROJECT_TIER_SUBSCRIPTION", "DATA_MANAGER_STORAGE_SUBSCRIPTION"])
              .describe("The Product Type\n"),
            flavour: zod
              .enum(["EVALUATION", "BRONZE", "SILVER", "GOLD"])
              .optional()
              .describe("The Product Type flavour. Not all products have flavours\n"),
            name: zod.string().optional().describe("The name of the Product\n"),
          }),
          organisation: zod.object({
            caller_is_member: zod
              .boolean()
              .describe("Whether the user making the API call is a member of the Unit"),
            id: zod.string().describe("The Organisation's unique ID"),
            name: zod.string().describe("The Organisation's name"),
            owner_id: zod
              .string()
              .optional()
              .describe(
                "The username of the Organisation's owner. Not all Organisations have an owner. The Default Organisation has no owner.",
              ),
            private: zod.boolean().describe("True if the Unit is private"),
            created: zod.iso.datetime({ offset: true }),
            default_product_privacy: zod
              .enum(["ALWAYS_PUBLIC", "ALWAYS_PRIVATE", "DEFAULT_PUBLIC", "DEFAULT_PRIVATE"])
              .describe("The Organisation's default product privacy setting"),
            users: zod
              .array(zod.object({ id: zod.string().describe("The user identity (username)") }))
              .describe("A list of users that are members of the Organisation"),
          }),
          unit: zod.object({
            caller_is_member: zod
              .boolean()
              .describe("Whether the user making the API call is a member of the Unit"),
            id: zod.string().describe("The Unit's unique identity"),
            name: zod.string().describe("The Unit's name"),
            owner_id: zod.string().describe("The Unit's owner (a username)"),
            private: zod.boolean().describe("True if the Unit is private"),
            created: zod.iso.datetime({ offset: true }),
            billing_day: zod.number().describe("The Unit's billing day"),
            default_product_privacy: zod
              .enum(["ALWAYS_PUBLIC", "ALWAYS_PRIVATE", "DEFAULT_PUBLIC", "DEFAULT_PRIVATE"])
              .describe("The Unit's default product privacy setting"),
            users: zod
              .array(zod.object({ id: zod.string().describe("The user identity (username)") }))
              .describe("A list of users that are members of the Unit"),
          }),
          storage: zod.object({
            size: zod.object({
              peak: zod
                .string()
                .describe(
                  "The humanised size of the peak storage used for the current day. The value is reset at the start of each day",
                ),
              current: zod
                .string()
                .describe(
                  "The humanised size of the current storage used for the current day and used to calculate the 'burn rate'",
                ),
              unit_size: zod
                .string()
                .describe(
                  "The humanised storage unit. The cost of storage is based on the daily peak of the number of units (or part thereof) used",
                ),
              units_used: zod.number().describe("The peak number of storage units used today"),
            }),
            coins: zod.object({
              used: zod
                .number()
                .describe(
                  "The number of coins currently committed for the current day. This is added to the accumulated coins at the start of each day",
                ),
              unit_cost: zod
                .number()
                .describe(
                  "The coin cost of a 'unit' of storage or part thereof. The unit size is defined in the storage section of the response",
                ),
            }),
          }),
          coins: zod.object({
            allowance: zod
              .number()
              .describe(
                "The billing allowance. When you exceed this during the current billing period the \*cost multiplier\* will increase",
              ),
            limit: zod
              .number()
              .describe(
                "The limit on your billing period spend. You can exceed the allowance but you cannot exceed the spend limit. Once reached the dependent may be restricted",
              ),
            used: zod
              .number()
              .describe(
                "The total number of coins consumed (in this billing period), excluding the coins that have been consumed for the current day",
              ),
            at_limit: zod
              .boolean()
              .describe(
                "True if the product is operating at or beyond its coin limit. When it is authority to perform actions using the product are severely limited.",
              ),
            current_burn_rate: zod
              .number()
              .describe(
                "The current burn rate, the approximate amount of coins you are currently consuming each day",
              ),
            billing_prediction: zod
              .number()
              .describe(
                "The predicted total billing period amount, if costs continue at the current burn rate until the end of the billing period. This is an estimate",
              ),
            billing_prediction_storage_contribution: zod
              .number()
              .describe(
                "The predicted storage contribution to the billing period amount, if costs continue at the current burn rate until the end of the billing period. This is an estimate",
              ),
            billing_day: zod
              .number()
              .describe(
                "The day of the month when the bill is due, and the end of the current billing period",
              ),
            allowance_multiplier: zod
              .number()
              .describe("A multiplier applied to your coin usage within your allowance"),
            overspend_multiplier: zod
              .number()
              .describe("A multiplier that will be applied to coin used beyond your allowance"),
            remaining_days: zod
              .number()
              .describe("The number of days remaining, in the current billing period"),
          }),
          claimable: zod.boolean().describe("True if the product can be (needs to be) claimed."),
        }),
        zod.object({
          product: zod.object({
            id: zod.string().describe("The Product ID\n"),
            created: zod.iso.datetime({ offset: true }),
            type: zod
              .enum(["DATA_MANAGER_PROJECT_TIER_SUBSCRIPTION", "DATA_MANAGER_STORAGE_SUBSCRIPTION"])
              .describe("The Product Type\n"),
            flavour: zod
              .enum(["EVALUATION", "BRONZE", "SILVER", "GOLD"])
              .optional()
              .describe("The Product Type flavour. Not all products have flavours\n"),
            name: zod.string().optional().describe("The name of the Product\n"),
          }),
          organisation: zod.object({
            caller_is_member: zod
              .boolean()
              .describe("Whether the user making the API call is a member of the Unit"),
            id: zod.string().describe("The Organisation's unique ID"),
            name: zod.string().describe("The Organisation's name"),
            owner_id: zod
              .string()
              .optional()
              .describe(
                "The username of the Organisation's owner. Not all Organisations have an owner. The Default Organisation has no owner.",
              ),
            private: zod.boolean().describe("True if the Unit is private"),
            created: zod.iso.datetime({ offset: true }),
            default_product_privacy: zod
              .enum(["ALWAYS_PUBLIC", "ALWAYS_PRIVATE", "DEFAULT_PUBLIC", "DEFAULT_PRIVATE"])
              .describe("The Organisation's default product privacy setting"),
            users: zod
              .array(zod.object({ id: zod.string().describe("The user identity (username)") }))
              .describe("A list of users that are members of the Organisation"),
          }),
          unit: zod.object({
            caller_is_member: zod
              .boolean()
              .describe("Whether the user making the API call is a member of the Unit"),
            id: zod.string().describe("The Unit's unique identity"),
            name: zod.string().describe("The Unit's name"),
            owner_id: zod.string().describe("The Unit's owner (a username)"),
            private: zod.boolean().describe("True if the Unit is private"),
            created: zod.iso.datetime({ offset: true }),
            billing_day: zod.number().describe("The Unit's billing day"),
            default_product_privacy: zod
              .enum(["ALWAYS_PUBLIC", "ALWAYS_PRIVATE", "DEFAULT_PUBLIC", "DEFAULT_PRIVATE"])
              .describe("The Unit's default product privacy setting"),
            users: zod
              .array(zod.object({ id: zod.string().describe("The user identity (username)") }))
              .describe("A list of users that are members of the Unit"),
          }),
          storage: zod.object({
            size: zod.object({
              peak: zod
                .string()
                .describe(
                  "The humanised size of the peak storage used for the current day. The value is reset at the start of each day",
                ),
              current: zod
                .string()
                .describe(
                  "The humanised size of the current storage used for the current day and used to calculate the 'burn rate'",
                ),
              unit_size: zod
                .string()
                .describe(
                  "The humanised storage unit. The cost of storage is based on the daily peak of the number of units (or part thereof) used",
                ),
              units_used: zod.number().describe("The peak number of storage units used today"),
            }),
            coins: zod.object({
              used: zod
                .number()
                .describe(
                  "The number of coins currently committed for the current day. This is added to the accumulated coins at the start of each day",
                ),
              unit_cost: zod
                .number()
                .describe(
                  "The coin cost of a 'unit' of storage or part thereof. The unit size is defined in the storage section of the response",
                ),
            }),
          }),
          coins: zod.object({
            allowance: zod
              .number()
              .describe(
                "The billing allowance. When you exceed this during the current billing period the \*cost multiplier\* will increase",
              ),
            limit: zod
              .number()
              .describe(
                "The limit on your billing period spend. You can exceed the allowance but you cannot exceed the spend limit. Once reached the dependent may be restricted",
              ),
            used: zod
              .number()
              .describe(
                "The total number of coins consumed (in this billing period), excluding the coins that have been consumed for the current day",
              ),
            at_limit: zod
              .boolean()
              .describe(
                "True if the product is operating at or beyond its coin limit. When it is authority to perform actions using the product are severely limited.",
              ),
            current_burn_rate: zod
              .number()
              .describe(
                "The current burn rate, the approximate amount of coins you are currently consuming each day",
              ),
            billing_prediction: zod
              .number()
              .describe(
                "The predicted total billing period amount, if costs continue at the current burn rate until the end of the billing period. This is an estimate",
              ),
            billing_prediction_storage_contribution: zod
              .number()
              .describe(
                "The predicted storage contribution to the billing period amount, if costs continue at the current burn rate until the end of the billing period. This is an estimate",
              ),
            billing_day: zod
              .number()
              .describe(
                "The day of the month when the bill is due, and the end of the current billing period",
              ),
            allowance_multiplier: zod
              .number()
              .describe("A multiplier applied to your coin usage within your allowance"),
            overspend_multiplier: zod
              .number()
              .describe("A multiplier that will be applied to coin used beyond your allowance"),
            remaining_days: zod
              .number()
              .describe("The number of days remaining, in the current billing period"),
          }),
          instance: zod.object({
            coins: zod.object({ used: zod.number().describe("The number of coins used\n") }),
          }),
          claimable: zod.boolean().describe("True if the product can be (needs to be) claimed."),
          claim: zod
            .object({
              id: zod
                .string()
                .describe("The service-specific ID that is using this Subscription\n"),
              name: zod.string().optional().describe("A name for the service-specific ID\n"),
            })
            .optional(),
        }),
      ]),
    )
    .describe("All the Products you have access to"),
});

/**
 * Gets details of a specific Product that you have access to.
 * @summary Gets a Product
 */
export const appApiProductGetProductPathProductIdRegExp = new RegExp(
  "^product-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiProductGetProductParams = zod.object({
  product_id: zod
    .string()
    .regex(appApiProductGetProductPathProductIdRegExp)
    .describe("A Product Identity"),
});

export const AppApiProductGetProductResponse = zod.object({
  product: zod
    .union([
      zod.object({
        product: zod.object({
          id: zod.string().describe("The Product ID\n"),
          created: zod.iso.datetime({ offset: true }),
          type: zod
            .enum(["DATA_MANAGER_PROJECT_TIER_SUBSCRIPTION", "DATA_MANAGER_STORAGE_SUBSCRIPTION"])
            .describe("The Product Type\n"),
          flavour: zod
            .enum(["EVALUATION", "BRONZE", "SILVER", "GOLD"])
            .optional()
            .describe("The Product Type flavour. Not all products have flavours\n"),
          name: zod.string().optional().describe("The name of the Product\n"),
        }),
        organisation: zod.object({
          caller_is_member: zod
            .boolean()
            .describe("Whether the user making the API call is a member of the Unit"),
          id: zod.string().describe("The Organisation's unique ID"),
          name: zod.string().describe("The Organisation's name"),
          owner_id: zod
            .string()
            .optional()
            .describe(
              "The username of the Organisation's owner. Not all Organisations have an owner. The Default Organisation has no owner.",
            ),
          private: zod.boolean().describe("True if the Unit is private"),
          created: zod.iso.datetime({ offset: true }),
          default_product_privacy: zod
            .enum(["ALWAYS_PUBLIC", "ALWAYS_PRIVATE", "DEFAULT_PUBLIC", "DEFAULT_PRIVATE"])
            .describe("The Organisation's default product privacy setting"),
          users: zod
            .array(zod.object({ id: zod.string().describe("The user identity (username)") }))
            .describe("A list of users that are members of the Organisation"),
        }),
        unit: zod.object({
          caller_is_member: zod
            .boolean()
            .describe("Whether the user making the API call is a member of the Unit"),
          id: zod.string().describe("The Unit's unique identity"),
          name: zod.string().describe("The Unit's name"),
          owner_id: zod.string().describe("The Unit's owner (a username)"),
          private: zod.boolean().describe("True if the Unit is private"),
          created: zod.iso.datetime({ offset: true }),
          billing_day: zod.number().describe("The Unit's billing day"),
          default_product_privacy: zod
            .enum(["ALWAYS_PUBLIC", "ALWAYS_PRIVATE", "DEFAULT_PUBLIC", "DEFAULT_PRIVATE"])
            .describe("The Unit's default product privacy setting"),
          users: zod
            .array(zod.object({ id: zod.string().describe("The user identity (username)") }))
            .describe("A list of users that are members of the Unit"),
        }),
        storage: zod.object({
          size: zod.object({
            peak: zod
              .string()
              .describe(
                "The humanised size of the peak storage used for the current day. The value is reset at the start of each day",
              ),
            current: zod
              .string()
              .describe(
                "The humanised size of the current storage used for the current day and used to calculate the 'burn rate'",
              ),
            unit_size: zod
              .string()
              .describe(
                "The humanised storage unit. The cost of storage is based on the daily peak of the number of units (or part thereof) used",
              ),
            units_used: zod.number().describe("The peak number of storage units used today"),
          }),
          coins: zod.object({
            used: zod
              .number()
              .describe(
                "The number of coins currently committed for the current day. This is added to the accumulated coins at the start of each day",
              ),
            unit_cost: zod
              .number()
              .describe(
                "The coin cost of a 'unit' of storage or part thereof. The unit size is defined in the storage section of the response",
              ),
          }),
        }),
        coins: zod.object({
          allowance: zod
            .number()
            .describe(
              "The billing allowance. When you exceed this during the current billing period the \*cost multiplier\* will increase",
            ),
          limit: zod
            .number()
            .describe(
              "The limit on your billing period spend. You can exceed the allowance but you cannot exceed the spend limit. Once reached the dependent may be restricted",
            ),
          used: zod
            .number()
            .describe(
              "The total number of coins consumed (in this billing period), excluding the coins that have been consumed for the current day",
            ),
          at_limit: zod
            .boolean()
            .describe(
              "True if the product is operating at or beyond its coin limit. When it is authority to perform actions using the product are severely limited.",
            ),
          current_burn_rate: zod
            .number()
            .describe(
              "The current burn rate, the approximate amount of coins you are currently consuming each day",
            ),
          billing_prediction: zod
            .number()
            .describe(
              "The predicted total billing period amount, if costs continue at the current burn rate until the end of the billing period. This is an estimate",
            ),
          billing_prediction_storage_contribution: zod
            .number()
            .describe(
              "The predicted storage contribution to the billing period amount, if costs continue at the current burn rate until the end of the billing period. This is an estimate",
            ),
          billing_day: zod
            .number()
            .describe(
              "The day of the month when the bill is due, and the end of the current billing period",
            ),
          allowance_multiplier: zod
            .number()
            .describe("A multiplier applied to your coin usage within your allowance"),
          overspend_multiplier: zod
            .number()
            .describe("A multiplier that will be applied to coin used beyond your allowance"),
          remaining_days: zod
            .number()
            .describe("The number of days remaining, in the current billing period"),
        }),
        claimable: zod.boolean().describe("True if the product can be (needs to be) claimed."),
      }),
      zod.object({
        product: zod.object({
          id: zod.string().describe("The Product ID\n"),
          created: zod.iso.datetime({ offset: true }),
          type: zod
            .enum(["DATA_MANAGER_PROJECT_TIER_SUBSCRIPTION", "DATA_MANAGER_STORAGE_SUBSCRIPTION"])
            .describe("The Product Type\n"),
          flavour: zod
            .enum(["EVALUATION", "BRONZE", "SILVER", "GOLD"])
            .optional()
            .describe("The Product Type flavour. Not all products have flavours\n"),
          name: zod.string().optional().describe("The name of the Product\n"),
        }),
        organisation: zod.object({
          caller_is_member: zod
            .boolean()
            .describe("Whether the user making the API call is a member of the Unit"),
          id: zod.string().describe("The Organisation's unique ID"),
          name: zod.string().describe("The Organisation's name"),
          owner_id: zod
            .string()
            .optional()
            .describe(
              "The username of the Organisation's owner. Not all Organisations have an owner. The Default Organisation has no owner.",
            ),
          private: zod.boolean().describe("True if the Unit is private"),
          created: zod.iso.datetime({ offset: true }),
          default_product_privacy: zod
            .enum(["ALWAYS_PUBLIC", "ALWAYS_PRIVATE", "DEFAULT_PUBLIC", "DEFAULT_PRIVATE"])
            .describe("The Organisation's default product privacy setting"),
          users: zod
            .array(zod.object({ id: zod.string().describe("The user identity (username)") }))
            .describe("A list of users that are members of the Organisation"),
        }),
        unit: zod.object({
          caller_is_member: zod
            .boolean()
            .describe("Whether the user making the API call is a member of the Unit"),
          id: zod.string().describe("The Unit's unique identity"),
          name: zod.string().describe("The Unit's name"),
          owner_id: zod.string().describe("The Unit's owner (a username)"),
          private: zod.boolean().describe("True if the Unit is private"),
          created: zod.iso.datetime({ offset: true }),
          billing_day: zod.number().describe("The Unit's billing day"),
          default_product_privacy: zod
            .enum(["ALWAYS_PUBLIC", "ALWAYS_PRIVATE", "DEFAULT_PUBLIC", "DEFAULT_PRIVATE"])
            .describe("The Unit's default product privacy setting"),
          users: zod
            .array(zod.object({ id: zod.string().describe("The user identity (username)") }))
            .describe("A list of users that are members of the Unit"),
        }),
        storage: zod.object({
          size: zod.object({
            peak: zod
              .string()
              .describe(
                "The humanised size of the peak storage used for the current day. The value is reset at the start of each day",
              ),
            current: zod
              .string()
              .describe(
                "The humanised size of the current storage used for the current day and used to calculate the 'burn rate'",
              ),
            unit_size: zod
              .string()
              .describe(
                "The humanised storage unit. The cost of storage is based on the daily peak of the number of units (or part thereof) used",
              ),
            units_used: zod.number().describe("The peak number of storage units used today"),
          }),
          coins: zod.object({
            used: zod
              .number()
              .describe(
                "The number of coins currently committed for the current day. This is added to the accumulated coins at the start of each day",
              ),
            unit_cost: zod
              .number()
              .describe(
                "The coin cost of a 'unit' of storage or part thereof. The unit size is defined in the storage section of the response",
              ),
          }),
        }),
        coins: zod.object({
          allowance: zod
            .number()
            .describe(
              "The billing allowance. When you exceed this during the current billing period the \*cost multiplier\* will increase",
            ),
          limit: zod
            .number()
            .describe(
              "The limit on your billing period spend. You can exceed the allowance but you cannot exceed the spend limit. Once reached the dependent may be restricted",
            ),
          used: zod
            .number()
            .describe(
              "The total number of coins consumed (in this billing period), excluding the coins that have been consumed for the current day",
            ),
          at_limit: zod
            .boolean()
            .describe(
              "True if the product is operating at or beyond its coin limit. When it is authority to perform actions using the product are severely limited.",
            ),
          current_burn_rate: zod
            .number()
            .describe(
              "The current burn rate, the approximate amount of coins you are currently consuming each day",
            ),
          billing_prediction: zod
            .number()
            .describe(
              "The predicted total billing period amount, if costs continue at the current burn rate until the end of the billing period. This is an estimate",
            ),
          billing_prediction_storage_contribution: zod
            .number()
            .describe(
              "The predicted storage contribution to the billing period amount, if costs continue at the current burn rate until the end of the billing period. This is an estimate",
            ),
          billing_day: zod
            .number()
            .describe(
              "The day of the month when the bill is due, and the end of the current billing period",
            ),
          allowance_multiplier: zod
            .number()
            .describe("A multiplier applied to your coin usage within your allowance"),
          overspend_multiplier: zod
            .number()
            .describe("A multiplier that will be applied to coin used beyond your allowance"),
          remaining_days: zod
            .number()
            .describe("The number of days remaining, in the current billing period"),
        }),
        instance: zod.object({
          coins: zod.object({ used: zod.number().describe("The number of coins used\n") }),
        }),
        claimable: zod.boolean().describe("True if the product can be (needs to be) claimed."),
        claim: zod
          .object({
            id: zod.string().describe("The service-specific ID that is using this Subscription\n"),
            name: zod.string().optional().describe("A name for the service-specific ID\n"),
          })
          .optional(),
      }),
    ])
    .describe("The Unit's Product"),
});

/**
 * You need access to the Product and, if the Product is *claimable* the claim must be removed before the Product can be removed. An example claimable Product is a `DATA_MANAGER_PROJECT_TIER_SUBSCRIPTION` where the *claimable* commodity is a Data Manager **Project**. In this case you will need to delete the Data Manager **Project** before you can delete the Account Server **Product**.
 * @summary Deletes an existing Product
 */
export const appApiProductDeletePathProductIdRegExp = new RegExp(
  "^product-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiProductDeleteParams = zod.object({
  product_id: zod
    .string()
    .regex(appApiProductDeletePathProductIdRegExp)
    .describe("A Product Identity"),
});

export const AppApiProductDeleteResponse = zod.void();

/**
 * Used to update some adjustable parameters of a Product, i.e. to extend its **Allowance** or **Limit**.
 *
 * You must be a member of the **Unit**, or its **Organisation**, or an administrator to use this method.
 *
 * At the moment we only support Products associated with the Data Manager, and these can be patched by changing the `name`, its coin `allowance` or `limit`
 * @summary Adjust an existing Product
 */
export const appApiProductPatchPathProductIdRegExp = new RegExp(
  "^product-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiProductPatchParams = zod.object({
  product_id: zod
    .string()
    .regex(appApiProductPatchPathProductIdRegExp)
    .describe("A Product Identity"),
});

export const appApiProductPatchBodyNameMax = 80;

export const AppApiProductPatchBody = zod.object({
  name: zod
    .string()
    .max(appApiProductPatchBodyNameMax)
    .optional()
    .describe("The name you want to give the Product"),
  allowance: zod
    .number()
    .min(1)
    .optional()
    .describe("The Product's built-in coin allowance. Product allowances cannot be reduced"),
  limit: zod
    .number()
    .min(1)
    .optional()
    .describe(
      "The Product's built-in coin limit. If set it must not be less than the allowance. If not set the allowance is used. The existing product limit cannot be reduced",
    ),
});

export const AppApiProductPatchResponse = zod.unknown();

# Local OpenAPI Inputs

Place downloaded API contracts here using these fixed filenames:

- `data-manager.yaml`
- `account-server.yaml`

The contracts are mutable local inputs and are ignored by Git. The generated clients under
`src/api/` are application source and are committed.

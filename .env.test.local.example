TEST_PORT=3000
BASE_URL="http://localhost:3000"
BASE_PATH='/data-manager-ui'
DATA_MANAGER_API_SERVER='...'
ACCOUNT_SERVER_API_SERVER='...'
BETTER_AUTH_SECRET='LONG_RANDOM_VALUE'
KEYCLOAK_URL='https://.../auth/realms/...'
KEYCLOAK_CLIENT_ID='...'
KEYCLOAK_CLIENT_SECRET='...'

# The live suites name the identities they sign in as — `tests/liveEnvironment.ts` for the user
# both of them use, `tests/journey/deployment.ts` for the collaborator the journey shares its
# project with — and read only their passwords from here.
DMIT_USER_A_PASSWORD="live user password"
DMIT_USER_B_PASSWORD="collaborator password"

/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * Account Server API
 * The Informatics Matters Account Server API.
 *
 * A service that provides access to the Account Server, which gives *registered* users access to and management of **Organisations**, **Units**, **Products**, **Users**, and **Assets**.
 *
 * OpenAPI spec version: 4.7
 */
import * as zod from "zod";

/**
 * Gets Assets you have access to.
 * @summary Gets Assets
 */
export const appApiAssetGetQueryUserIdMin = 3;
export const appApiAssetGetQueryUserIdMax = 80;

export const appApiAssetGetQueryUserIdRegExp = new RegExp("^\\w(?:\\w*(?:[@.-]\\w+)?)*$");
export const appApiAssetGetQueryProductIdRegExp = new RegExp(
  "^product-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);
export const appApiAssetGetQueryUnitIdRegExp = new RegExp(
  "^unit-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);
export const appApiAssetGetQueryOrgIdRegExp = new RegExp(
  "^org-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiAssetGetQueryParams = zod.object({
  user_id: zod
    .string()
    .min(appApiAssetGetQueryUserIdMin)
    .max(appApiAssetGetQueryUserIdMax)
    .regex(appApiAssetGetQueryUserIdRegExp)
    .optional()
    .describe("A User Identity"),
  product_id: zod
    .string()
    .regex(appApiAssetGetQueryProductIdRegExp)
    .optional()
    .describe("A Product Identity"),
  unit_id: zod
    .string()
    .regex(appApiAssetGetQueryUnitIdRegExp)
    .optional()
    .describe("A Unit Identity"),
  org_id: zod
    .string()
    .regex(appApiAssetGetQueryOrgIdRegExp)
    .optional()
    .describe("An Organisation Identity"),
});

export const AppApiAssetGetResponse = zod.object({
  assets: zod
    .array(
      zod.object({
        creator: zod.string(),
        id: zod.string(),
        name: zod.string(),
        scope: zod.enum(["USER", "PRODUCT", "UNIT", "ORGANISATION", "GLOBAL"]),
        scope_id: zod.string(),
        secret: zod.boolean(),
        disabled: zod.boolean(),
        content: zod.string(),
        created: zod.iso.datetime({ offset: true }),
        content_modified: zod.iso.datetime({ offset: true }).optional(),
        description: zod.string().optional(),
        merchants: zod.array(
          zod.object({
            id: zod.number().describe("The unique ID of the Service"),
            created: zod.iso.datetime({ offset: true }),
            kind: zod.enum(["DATA_MANAGER"]).describe("The kind of Service"),
            name: zod.string().describe("The name assigned to the Service"),
            api_hostname: zod.string().describe("The hostname used by the Service"),
          }),
        ),
      }),
    )
    .describe("A list of Assets\n"),
  count: zod.number().describe("The number of Assets returned\n"),
});

/**
 * Creates an Asset, which can be attached to a **User** account, a **Product**, **Unit**, **Organisation** or can be made **Global**
 *
 * Assets are text and file resources that can be requested by **Merchants**. The Data Manager **Merchant** uses assets to provide additional information that's often sensitive to **Job** (and **Application**) **Instances**.
 *
 * Assets must be *enabled* before they can be used.
 *
 * They must also be *attached* to a **Merchant** before the **Merchant** can ue them. Assets are only available to merchants if they are enabled and attached.
 * @summary Create an Asset
 */
export const appApiAssetCreateBodyNameMax = 80;

export const appApiAssetCreateBodyNameRegExp = new RegExp("^[a-z0-9-]{1,63}$");

export const AppApiAssetCreateBody = zod.object({
  name: zod
    .string()
    .max(appApiAssetCreateBodyNameMax)
    .regex(appApiAssetCreateBodyNameRegExp)
    .describe(
      'The name of the asset. This must be unique within its scope. For example, only one asset can be called \"asset-1\" within a given `UNIT`. Asset names must be valid RFC 1123 Label Names',
    ),
  content_file: zod.instanceof(File).optional(),
  content_string: zod
    .string()
    .optional()
    .describe(
      "The textual content of the asset. You must provide a value here or in \*\*content_file\*\* but not both",
    ),
  scope: zod
    .enum(["USER", "PRODUCT", "UNIT", "ORGANISATION", "GLOBAL"])
    .describe(
      "An enumeration of the scope of the asset. This can be one of `USER`, `PRODUCT`, `UNIT`, `ORGANISATION`, or `GLOBAL`. Assets that are not `GLOBAL` need a scope ID.",
    ),
  scope_id: zod
    .string()
    .optional()
    .describe(
      "The unique identity based on the Scope of the asset. For example, this will be the Unit ID if it's a UNIT. A scope_id is required if the scope is USER as it will be automatically set to your username. Global assets do not have a scope.",
    ),
  secret: zod
    .boolean()
    .describe(
      "Is this a secret asset? Secret assets are not revealed in a subsequent \*\*GET\*\* but are revealed to merchants they are connected to.",
    ),
  description: zod.string().optional().describe("An optional description for the Asset"),
});

export const AppApiAssetCreateResponse = zod.object({
  id: zod.string().describe("The Asset ID\n"),
});

/**
 * Gets a known Asset
 * @summary Gets an Asset
 */
export const appApiAssetGetByIdPathAssetIdRegExp = new RegExp(
  "^asset-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiAssetGetByIdParams = zod.object({
  asset_id: zod.string().regex(appApiAssetGetByIdPathAssetIdRegExp).describe("An Asset Identity"),
});

export const AppApiAssetGetByIdResponse = zod.object({
  creator: zod.string(),
  id: zod.string(),
  name: zod.string(),
  scope: zod.enum(["USER", "PRODUCT", "UNIT", "ORGANISATION", "GLOBAL"]),
  scope_id: zod.string(),
  secret: zod.boolean(),
  disabled: zod.boolean(),
  content: zod.string(),
  created: zod.iso.datetime({ offset: true }),
  content_modified: zod.iso.datetime({ offset: true }).optional(),
  description: zod.string().optional(),
  merchants: zod.array(
    zod.object({
      id: zod.number().describe("The unique ID of the Service"),
      created: zod.iso.datetime({ offset: true }),
      kind: zod.enum(["DATA_MANAGER"]).describe("The kind of Service"),
      name: zod.string().describe("The name assigned to the Service"),
      api_hostname: zod.string().describe("The hostname used by the Service"),
    }),
  ),
});

/**
 * Used to update or replace Asset *Content* or *Description*.
 *
 * The content of an Asset currently in use will not be affected by a change made here. If a **Merchant** has already obtained the Asset it may continue to use the original value until the **Merchant** requests the **Asset** again.
 *
 * Anyone can alter a `GLOBAL` asset. For other *scopes* you will need to be the user for `USER` scoped assets, a member of the **Unit** for `UNIT` and `PRODUCT` scoped assets, or a member of the **Organisation** for `ORGANISATION` scoped assets. Administrators can patch any asset.
 * @summary Adjust an existing Asset
 */
export const appApiAssetPatchPathAssetIdRegExp = new RegExp(
  "^asset-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiAssetPatchParams = zod.object({
  asset_id: zod.string().regex(appApiAssetPatchPathAssetIdRegExp).describe("An Asset Identity"),
});

export const AppApiAssetPatchBody = zod.object({
  content_file: zod.instanceof(File).optional(),
  content_string: zod
    .string()
    .optional()
    .describe("The textual content of the asset. You must provide a value here or in content_file"),
  description: zod.string().optional().describe("An optional description for the Asset"),
});

export const AppApiAssetPatchResponse = zod.unknown();

/**
 * Deletes a known Asset. Assets that are attached to **Merchants** cannot be deleted
 *
 * Anyone can alter a `GLOBAL` asset. For other *scopes* you will need to be the user for `USER` scoped assets, a member of the **Unit** for `UNIT` and `PRODUCT` scoped assets, or a member of the **Organisation** for `ORGANISATION` scoped assets. Administrators can patch any asset.
 * @summary Deletes an Asset
 */
export const appApiAssetDeletePathAssetIdRegExp = new RegExp(
  "^asset-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiAssetDeleteParams = zod.object({
  asset_id: zod.string().regex(appApiAssetDeletePathAssetIdRegExp).describe("An Asset Identity"),
});

export const AppApiAssetDeleteResponse = zod.void();

/**
 * Disables a known Asset
 *
 * Anyone can alter a `GLOBAL` asset. For other *scopes* you will need to be the user for `USER` scoped assets, a member of the **Unit** for `UNIT` and `PRODUCT` scoped assets, or a member of the **Organisation** for `ORGANISATION` scoped assets. Administrators can patch any asset.
 * @summary Disables an Asset
 */
export const appApiAssetDisablePathAssetIdRegExp = new RegExp(
  "^asset-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiAssetDisableParams = zod.object({
  asset_id: zod.string().regex(appApiAssetDisablePathAssetIdRegExp).describe("An Asset Identity"),
});

export const AppApiAssetDisableResponse = zod.void();

/**
 * Enables a known Asset
 *
 * Anyone can alter a `GLOBAL` asset. For other *scopes* you will need to be the user for `USER` scoped assets, a member of the **Unit** for `UNIT` and `PRODUCT` scoped assets, or a member of the **Organisation** for `ORGANISATION` scoped assets. Administrators can patch any asset.
 * @summary Enables an Asset
 */
export const appApiAssetEnablePathAssetIdRegExp = new RegExp(
  "^asset-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiAssetEnableParams = zod.object({
  asset_id: zod.string().regex(appApiAssetEnablePathAssetIdRegExp).describe("An Asset Identity"),
});

export const AppApiAssetEnableResponse = zod.void();

/**
 * Attaches an Asset to a **Merchant**. This allows the **Merchant** to query the Asset. **Merchants** cannot obtain Assets that are not attached to them.
 *
 * Anyone can alter a `GLOBAL` asset. For other *scopes* you will need to be the user for `USER` scoped assets, a member of the **Unit** for `UNIT` and `PRODUCT` scoped assets, or a member of the **Organisation** for `ORGANISATION` scoped assets. Administrators can patch any asset.
 * @summary Attaches an Asset to a Merchant
 */
export const appApiAssetAttachPathAssetIdRegExp = new RegExp(
  "^asset-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiAssetAttachParams = zod.object({
  asset_id: zod.string().regex(appApiAssetAttachPathAssetIdRegExp).describe("An Asset Identity"),
});

export const AppApiAssetAttachQueryParams = zod.object({
  m_id: zod.number().min(1).optional().describe("A Merchant Identity"),
});

export const AppApiAssetAttachResponse = zod.void();

/**
 * Detaches an Asset from a **Merchant**
 *
 * Anyone can alter a `GLOBAL` asset. For other *scopes* you will need to be the user for `USER` scoped assets, a member of the **Unit** for `UNIT` and `PRODUCT` scoped assets, or a member of the **Organisation** for `ORGANISATION` scoped assets. Administrators can patch any asset.
 * @summary Detaches an Asset from a Merchant
 */
export const appApiAssetDetachPathAssetIdRegExp = new RegExp(
  "^asset-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiAssetDetachParams = zod.object({
  asset_id: zod.string().regex(appApiAssetDetachPathAssetIdRegExp).describe("An Asset Identity"),
});

export const AppApiAssetDetachQueryParams = zod.object({
  m_id: zod.number().min(1).optional().describe("A Merchant Identity"),
});

export const AppApiAssetDetachResponse = zod.void();

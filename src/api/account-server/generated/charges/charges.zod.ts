/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * Account Server API
 * The Informatics Matters Account Server API.
 *
 * A service that provides access to the Account Server, which gives *registered* users access to and management of **Organisations**, **Units**, **Products**, **Users**, and **Assets**.
 *
 * OpenAPI spec version: 4.7
 */
import * as zod from "zod";

/**
 * Gets global Charges.
 *
 * You need admin rights to use this path.
 * @summary Gets charges for all Organisations
 */
export const appApiAdminGetChargesQueryPbpMin = -23;
export const appApiAdminGetChargesQueryPbpMax = 0;

export const AppApiAdminGetChargesQueryParams = zod.object({
  from: zod.iso
    .date()
    .optional()
    .describe(
      "A date from which to retrieve assets. Any asset that occurs on ar after this date will be returned. UTC is assumed if no timezone is provided",
    ),
  until: zod.iso
    .date()
    .optional()
    .describe(
      "A date where assets prior to this will be returned. Any asset that occurs prior this date will be returned. UTC is assumed if no timezone is provided",
    ),
  pbp: zod
    .number()
    .min(appApiAdminGetChargesQueryPbpMin)
    .max(appApiAdminGetChargesQueryPbpMax)
    .optional()
    .describe(
      'A charge Prior Billing Period, 0 implies \"current\" and -23 is for the period 23 months ago.',
    ),
});

export const AppApiAdminGetChargesResponse = zod.object({
  coins: zod.string(),
  count: zod.number(),
  summary: zod.array(
    zod.object({
      type: zod.enum(["PROCESSING", "STORAGE"]).describe("The type of charge"),
      coins: zod.string().describe("The cost, in coins of the charge"),
    }),
  ),
  organisation_charges: zod.array(
    zod.object({
      organisation_id: zod.string(),
      name: zod.string(),
      summary: zod.array(
        zod.object({
          type: zod.enum(["PROCESSING", "STORAGE"]).describe("The type of charge"),
          coins: zod.string().describe("The cost, in coins of the charge"),
        }),
      ),
    }),
  ),
});

/**
 * Gets a Organisation charges.
 *
 * The Organisation cannot be the **Default Organisation** and you need to be a member of the Organisation to use this endpoint
 * @summary Gets charges for all Units in an Organisation
 */
export const appApiOrganisationGetChargesPathOrgIdRegExp = new RegExp(
  "^org-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiOrganisationGetChargesParams = zod.object({
  org_id: zod
    .string()
    .regex(appApiOrganisationGetChargesPathOrgIdRegExp)
    .describe("An Organisation Identity"),
});

export const appApiOrganisationGetChargesQueryPbpMin = -23;
export const appApiOrganisationGetChargesQueryPbpMax = 0;

export const AppApiOrganisationGetChargesQueryParams = zod.object({
  from: zod.iso
    .date()
    .optional()
    .describe(
      "A date from which to retrieve assets. Any asset that occurs on ar after this date will be returned. UTC is assumed if no timezone is provided",
    ),
  until: zod.iso
    .date()
    .optional()
    .describe(
      "A date where assets prior to this will be returned. Any asset that occurs prior this date will be returned. UTC is assumed if no timezone is provided",
    ),
  pbp: zod
    .number()
    .min(appApiOrganisationGetChargesQueryPbpMin)
    .max(appApiOrganisationGetChargesQueryPbpMax)
    .optional()
    .describe(
      'A charge Prior Billing Period, 0 implies \"current\" and -23 is for the period 23 months ago.',
    ),
});

export const appApiOrganisationGetChargesResponseUnitChargesItemBillingDayMax = 28;

export const AppApiOrganisationGetChargesResponse = zod.object({
  organisation_id: zod.string(),
  name: zod.string(),
  coins: zod.string(),
  summary: zod.array(
    zod.object({
      type: zod.enum(["PROCESSING", "STORAGE"]).describe("The type of charge"),
      coins: zod.string().describe("The cost, in coins of the charge"),
    }),
  ),
  unit_charges: zod.array(
    zod.object({
      unit_id: zod.string(),
      name: zod.string(),
      billing_day: zod
        .number()
        .min(1)
        .max(appApiOrganisationGetChargesResponseUnitChargesItemBillingDayMax),
      from: zod.iso.date().describe("The start of the charge period"),
      until: zod.iso.date().describe("The date where of first day after the charge period"),
      summary: zod.array(
        zod.object({
          type: zod.enum(["PROCESSING", "STORAGE"]).describe("The type of charge"),
          coins: zod.string().describe("The cost, in coins of the charge"),
        }),
      ),
    }),
  ),
});

/**
 * Get the charges made against a Product with optional **from** (inclusive) and **until** (exclusive) dates. If no dates are provided, the charges for the current billing period are returned.
 *
 * Dates are interpreted using the Python `dateutil` parser, so the input strings are extremely flexible with, for example, `1 December 2021` as an acceptable input.
 *
 * **From** must be a date (day) prior to **until**. For example, to see the charges for the 11th July 2022 set **from** to `11 July 2022` and **until** to `12 July 2022`. As an alternative to **From** and **Until** you can provide a **Prior Billing Period**, a value that identifies the prior period to retrieve. A value of `-1` would indicate the *prior period* (month) with an oldest retrieval value of `-23` allowing you to obtain charges for up to two years.
 *
 * You need to be part of the **Unit** or **Organisation** to use this method
 * @summary Get charges made against a Product
 */
export const appApiProductGetChargesPathProductIdRegExp = new RegExp(
  "^product-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiProductGetChargesParams = zod.object({
  product_id: zod
    .string()
    .regex(appApiProductGetChargesPathProductIdRegExp)
    .describe("A Product Identity"),
});

export const appApiProductGetChargesQueryPbpMin = -23;
export const appApiProductGetChargesQueryPbpMax = 0;

export const AppApiProductGetChargesQueryParams = zod.object({
  from: zod.iso
    .date()
    .optional()
    .describe(
      "A date from which to retrieve assets. Any asset that occurs on ar after this date will be returned. UTC is assumed if no timezone is provided",
    ),
  until: zod.iso
    .date()
    .optional()
    .describe(
      "A date where assets prior to this will be returned. Any asset that occurs prior this date will be returned. UTC is assumed if no timezone is provided",
    ),
  pbp: zod
    .number()
    .min(appApiProductGetChargesQueryPbpMin)
    .max(appApiProductGetChargesQueryPbpMax)
    .optional()
    .describe(
      'A charge Prior Billing Period, 0 implies \"current\" and -23 is for the period 23 months ago.',
    ),
});

export const appApiProductGetChargesResponseBillingDayMax = 28;

export const AppApiProductGetChargesResponse = zod.object({
  product_id: zod.string(),
  product_type: zod.enum([
    "DATA_MANAGER_PROJECT_TIER_SUBSCRIPTION",
    "DATA_MANAGER_STORAGE_SUBSCRIPTION",
  ]),
  billing_day: zod.number().min(1).max(appApiProductGetChargesResponseBillingDayMax),
  claimable: zod.boolean().describe("True if the product can be (needs to be) claimed."),
  claim: zod
    .object({
      id: zod.string().describe("The service-specific ID that is using this Subscription\n"),
      name: zod.string().optional().describe("A name for the service-specific ID\n"),
    })
    .optional(),
  count: zod.number(),
  from: zod.iso.date().describe("The start of the charge period"),
  until: zod.iso.date().describe("The date where of first day after the charge period"),
  coins: zod
    .string()
    .describe("The total number of coins consumed by this product for the invoice period"),
  processing_charges: zod.array(
    zod.object({
      merchant_name: zod.string(),
      merchant_kind: zod.enum(["DATA_MANAGER"]),
      merchant_api_hostname: zod.string(),
      closed: zod.iso
        .datetime({ offset: true })
        .optional()
        .describe("The date when the process stopped"),
      final: zod
        .boolean()
        .describe(
          "True if no further change to the charges can occur. Typically True after the charge has been closed for a pre-configured period of time.",
        ),
      post_final_charges: zod
        .boolean()
        .optional()
        .describe("True if charges were received after the charge record was finalised"),
      charge: zod.object({
        id: zod.number().describe("The charge record number"),
        sqn: zod.number().describe("The most recent sequence number for this charge"),
        name: zod.string().optional(),
        username: zod.string(),
        timestamp: zod.iso
          .datetime({ offset: true })
          .describe("The date and time of the processing charge"),
        coins: zod.string().describe("The coin-cost of the storage"),
        additional_data: zod.record(zod.string(), zod.unknown()).optional(),
      }),
    }),
  ),
  storage_charges: zod.object({
    num_items: zod.number(),
    items: zod.array(
      zod.object({
        item_number: zod.number(),
        date: zod.iso.date().describe("The date and time of the processing charge"),
        coins: zod.string().describe("The coin-cost of the storage"),
        current_burn_rate: zod
          .string()
          .optional()
          .describe(
            "The current burn rate, the approximate amount of coins you are currently consuming each day",
          ),
        closed: zod.iso
          .datetime({ offset: true })
          .optional()
          .describe("The date when the charges concluded"),
        additional_data: zod.record(zod.string(), zod.unknown()).optional(),
      }),
    ),
  }),
});

/**
 * Get the charges made against a Unit with optional **from** (inclusive) and **until** (exclusive) dates. If no dates are provided, the charges for the current billing period are returned.
 * Dates are interpreted using the Python `dateutil` parser, so the input strings are extremely flexible with, for example, `1 December 2021` as an acceptable input.
 *
 * **From** must be a date (day) prior to **until**. For example, to see the charges for the 11th July 2022 set **from** to `11 July 2022` and **until** to `12 July 2022`. As an alternative to **From** and **Until** you can provide a **Prior Billing Period**, a value that identifies the prior period to retrieve. A value of `-1` would indicate the *prior period* (month) with an oldest retrieval value of `-23` allowing you to obtain charges for up to two years.
 *
 * You need to be part of the **Unit** or **Organisation** to use this method
 * @summary Get charges made against a Unit
 */
export const appApiUnitGetChargesPathUnitIdRegExp = new RegExp(
  "^unit-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiUnitGetChargesParams = zod.object({
  unit_id: zod.string().regex(appApiUnitGetChargesPathUnitIdRegExp).describe("A Unit Identity"),
});

export const appApiUnitGetChargesQueryPbpMin = -23;
export const appApiUnitGetChargesQueryPbpMax = 0;

export const AppApiUnitGetChargesQueryParams = zod.object({
  from: zod.iso
    .date()
    .optional()
    .describe(
      "A date from which to retrieve assets. Any asset that occurs on ar after this date will be returned. UTC is assumed if no timezone is provided",
    ),
  until: zod.iso
    .date()
    .optional()
    .describe(
      "A date where assets prior to this will be returned. Any asset that occurs prior this date will be returned. UTC is assumed if no timezone is provided",
    ),
  pbp: zod
    .number()
    .min(appApiUnitGetChargesQueryPbpMin)
    .max(appApiUnitGetChargesQueryPbpMax)
    .optional()
    .describe(
      'A charge Prior Billing Period, 0 implies \"current\" and -23 is for the period 23 months ago.',
    ),
});

export const appApiUnitGetChargesResponseBillingDayMax = 28;

export const AppApiUnitGetChargesResponse = zod.object({
  caller_is_member: zod
    .boolean()
    .describe("Whether the user making the API call is a member of the Unit"),
  unit_id: zod.string(),
  name: zod.string().optional(),
  owner_id: zod.string().describe("The Unit's owner (a username)"),
  private: zod.boolean().describe("True if the Unit is private"),
  created: zod.iso.datetime({ offset: true }),
  coins: zod.string(),
  count: zod.number(),
  billing_day: zod.number().min(1).max(appApiUnitGetChargesResponseBillingDayMax),
  from: zod.iso.date().describe("The start of the charge period"),
  until: zod.iso.date().describe("The date where of first day after the charge period"),
  summary: zod.object({
    charges: zod.array(
      zod.object({
        type: zod.enum(["PROCESSING", "STORAGE"]).describe("The type of charge"),
        coins: zod.string().describe("The cost, in coins of the charge"),
      }),
    ),
  }),
  products: zod.array(
    zod.object({
      product_id: zod.string(),
      product_type: zod.string(),
      charges: zod.array(
        zod.object({
          type: zod.enum(["PROCESSING", "STORAGE"]).describe("The type of charge"),
          coins: zod.string().describe("The cost, in coins of the charge"),
        }),
      ),
    }),
  ),
});

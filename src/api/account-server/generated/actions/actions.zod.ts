/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * Account Server API
 * The Informatics Matters Account Server API.
 *
 * A service that provides access to the Account Server, which gives *registered* users access to and management of **Organisations**, **Units**, **Products**, **Users**, and **Assets**.
 *
 * OpenAPI spec version: 4.7
 */
import * as zod from "zod";

/**
 * This endpoint delivers Action records (protocol buffers) that can be selected based on a date and time range and limited to merchants, organisations, Units, or Products. You can receive Action content as a `PROTOCOL_STRING` (the default), or `JSON_STRING`. If you do not provide a `format` `PROTOCOL_STRING` will be assumed.
 *
 * You can provide a `m_id`, `org_id`, `unit_id`, `product_id`, but only one.
 *
 * You can also provide `from` and `until` dates to limit the response to Actions that occur on the days you select. Actions are returned that are not before the `from` data but are before the `until` date. If neither `from` or `until` is provided `from` is assumed to be from _today_ until _tomorrow_.
 *
 * You need admin privilege to use this endpoint.
 * @summary Gets Actions held on the Account Server
 */

export const appApiActionGetQueryOrgIdRegExp = new RegExp(
  "^org-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);
export const appApiActionGetQueryProductIdRegExp = new RegExp(
  "^product-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);
export const appApiActionGetQueryUnitIdRegExp = new RegExp(
  "^unit-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$",
);

export const AppApiActionGetQueryParams = zod.object({
  format: zod
    .enum(["PROTOCOL_STRING", "JSON_STRING"])
    .optional()
    .describe("The required format for delivered content."),
  from: zod.iso
    .date()
    .optional()
    .describe(
      "A date from which to retrieve assets. Any asset that occurs on ar after this date will be returned. UTC is assumed if no timezone is provided",
    ),
  m_id: zod.number().min(1).optional().describe("A Merchant Identity"),
  org_id: zod
    .string()
    .regex(appApiActionGetQueryOrgIdRegExp)
    .optional()
    .describe("An Organisation Identity"),
  product_id: zod
    .string()
    .regex(appApiActionGetQueryProductIdRegExp)
    .optional()
    .describe("A Product Identity"),
  until: zod.iso
    .date()
    .optional()
    .describe(
      "A date where assets prior to this will be returned. Any asset that occurs prior this date will be returned. UTC is assumed if no timezone is provided",
    ),
  unit_id: zod
    .string()
    .regex(appApiActionGetQueryUnitIdRegExp)
    .optional()
    .describe("A Unit Identity"),
});

export const AppApiActionGetResponse = zod.object({
  actions: zod
    .array(
      zod.object({
        id: zod.number(),
        action: zod.string(),
        content: zod.string(),
        timestamp: zod.iso.datetime({ offset: true }),
        merchant: zod
          .object({
            id: zod.number().describe("The unique ID of the Merchant"),
            kind: zod.enum(["DATA_MANAGER"]).describe("The kind of Service"),
            name: zod.string().describe("The name assigned to the Service"),
          })
          .optional(),
        product_id: zod.string().optional(),
        unit_id: zod.string().optional(),
        org_id: zod.string().optional(),
      }),
    )
    .describe("A list of Actions\n"),
  count: zod.number().describe("The number of Actions returned\n"),
});
